#Task count the occurence of all vowels in a string
msg = input('Enter a sentence ')
#output:
# a-1
print ('a are :', msg. count("A"))

# e- 4
print('e are:', msg.count("e"))

# i -2
print('i are :', msg.count("i"))

for vowel in 'aeiou':
    print(vowel1,sentence.lower().count(vowel))
    
